import { apiTodoInterfaces } from './api-todo-interfaces';

describe('apiTodoInterfaces', () => {
  it('should work', () => {
    expect(apiTodoInterfaces()).toEqual('api-todo-interfaces');
  });
});
